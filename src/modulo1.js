export const name = 'Marcus';
export const lastName = 'Marques';
export const age = 22;
const cpf = 'asdasd';

export function soma(x, y) {
    return x+y;
}

export default (x, y) => x * y;

// export {name, lastName as sobrenome, age, soma };